<div class="form-floating mb-3">
    <input type="{{ type || 'text' }}" name="{{ name }}" id="{{ name }}" class="{{ cls || 'form-control' }}" placeholder="{{ placeholder }}">
    <label for="{{ name }}">{{ name }}</label>
</div>

@inputError(name)
  @each(message in $messages)
    <p class="text-danger">{{ message }}</p>
  @end
@end
